# Dependencies / build output (monorepo-wide)
node_modules/
pnpm-lock.yaml   # layihə npm işlədir (package-lock.json)
dist/
.next/
build/
coverage/

# WhatsApp sessiyası (whatsapp-web.js LocalAuth) — məxfidir, commit olunmamalıdır
.wwebjs_auth/
.wwebjs_cache/
apps/api/.wwebjs_auth/
apps/api/.wwebjs_cache/

# Runtime uploads
apps/api/uploads/

# Environment — never commit real secrets
.env
.env.*
!.env.example

# OS / editor
.DS_Store
Thumbs.db
*.log
.vscode/
